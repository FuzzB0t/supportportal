<div id="home" class="col-md-12">
  <div class="margin col-md-1"></div>
    <div class="col-md-11">
    <% if user_signed_in? %>
      <h4>User Actions:</h4>
        <ul>
          <li>
            <%= link_to "Action", "#", class: "" %>
          </li>
        </ul>
    <% elsif admin_signed_in? %>
      <h2> Admin </h2>
    <% else %>
      <h2>Not Logged In</h2>
    <% end %>
  </div>
</div>